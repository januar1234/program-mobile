void main() {
  // Membuat set dengan nama "januar" untuk menyimpan nama-nama
  Set<String> januarNames = {'Aji', 'Budi', 'Cici', 'Aji'}; // 'Aji' akan otomatis dihapus karena set tidak mengizinkan duplikasi

  // Menampilkan isi set
  print('Nama-nama dalam set januar: ' + januarNames.toString());

  // Menambahkan nama baru
  januarNames.add('Doni');
  print('Setelah menambahkan Doni: ' + januarNames.toString());

  // Menghapus nama
  januarNames.remove('Budi');
  print('Setelah menghapus Budi: ' + januarNames.toString());

  // Mengecek apakah nama ada dalam set
  print('Apakah Cici ada dalam set? ' + (januarNames.contains('Cici') ? 'Ya' : 'Tidak'));
  print('Apakah Aji ada dalam set? ' + (januarNames.contains('Aji') ? 'Ya' : 'Tidak'));

  // Menampilkan jumlah nama dalam set
  print('Jumlah nama dalam set januar: ' + januarNames.length.toString());
}
